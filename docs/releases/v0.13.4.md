# Release v0.13.4

## Ziel

Korrektur der öffentlichen `migration.php`, die bei bestimmten Plesk/open_basedir-Konfigurationen fälschlich meldete, dass Dateien außerhalb von `public/` nicht geladen werden können.

## Änderung

Die zusätzliche Vorprüfung mit `is_readable()` wurde entfernt. Die Datei lädt `app/Support/bootstrap.php` jetzt direkt wie `setup.php`.

## Datenbank

Keine neue Migration.

## Tests

- PHP-Syntaxprüfung für `public/migration.php`.
- PHP-Syntaxprüfung für alle PHP-Dateien.
- ZIP-Prüfung.

## Hinweis

`public/migration.php` nach erfolgreichem Lauf löschen.
