# Release v0.14.10

## Zweck

Behebt den Migrationsfehler in `2026_06_25_000026_import_birthdate_rank_dominance_fix.php`.

## Ursache

Die Migration schrieb in `app_versions.installed_at`. Die Tabelle verwendet in diesem Projekt aber `applied_at`, `notes` und `created_at`.

## Änderung

Die Migration trägt die Version jetzt über `app_versions.applied_at` ein.

## DB

Keine neue Migration. Die bestehende, fehlgeschlagene Migration 000026 wurde korrigiert.
