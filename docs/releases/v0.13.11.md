# v0.13.11 - Migration 000019 Bugfix

## Ziel

Korrektur der Migration `2026_06_25_000019_limit_all_day_modules_to_camp_days.php`.

## Behoben

- Die Migration hatte versehentlich nicht das standardisierte Migrationsformat mit `id` und `up` zurückgegeben.
- Dadurch meldete der MigrationRunner `Invalid migration file`.
- Zusätzlich wurde der Versionseintrag auf das vorhandene Schema von `app_versions` korrigiert.

## Datenbank

Keine neue Migration. Die bestehende Migration `000019` wurde korrigiert, weil sie noch nicht erfolgreich gelaufen ist.

## Test

- PHP-Syntaxprüfung erfolgreich.
- Migration gibt wieder ein Array mit `id` und `up` zurück.
